"""Desktop converter app for JDA → Pine.

PySide6 GUI that wraps the v2 pipeline. v2 stays a pure library; this
package is the user-facing layer that opens RTFs, runs conversions in
a background thread, and lets the mapper accept or reject LLM
suggestions.
"""

__version__ = "0.1.0"
